//to import file only writing folder name is enough because inside folder index.js file executes automatically and index.js file exporting the perticular Component
import Button from "./button";
import Footer from "./footer";
import Heading from "./heading";
import Input from "./input";
import TodoItem from "./todoitem";

export { Button, Footer, Heading, Input, TodoItem };
